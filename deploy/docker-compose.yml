services:
  backend:
    build:
      context: ../backend
    image: offerforge-backend:local
    container_name: offerforge-backend
    restart: unless-stopped
    environment:
      SERVER_PORT: 8080
    ports:
      - "${BACKEND_PORT:-8080}:8080"
